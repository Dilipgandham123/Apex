"use client";
import type { AdminDashboard } from "@hyd/contracts";
import Link from "next/link";
import { useEffect, useState } from "react";
import { apiFetch } from "@/lib/api";
import styles from "./admin.module.css";
const money=(value:string)=>`₹${Number(value).toLocaleString("en-IN")}`;
export default function AdminDashboardPage(){const[data,setData]=useState<AdminDashboard|null>(null),[message,setMessage]=useState("Loading current operations…");useEffect(()=>{void apiFetch<AdminDashboard>("/reports/dashboard").then(result=>{setData(result);setMessage("");}).catch(error=>setMessage(error instanceof Error?error.message:"Dashboard could not be loaded."));},[]);if(!data)return <div className={styles.page}><p className={styles.status}>{message}</p></div>;const e=data.exceptions;return <div className={styles.page}><header className={styles.pageHeader}><div><span>LIVE OPERATIONS</span><h1>School dashboard</h1><p>Current activity and records that need action, reconciled from lessons, kilometre entries, payments, complaints and fleet data.</p></div><Link className={styles.primaryLink} href="/admin/reports">Open reports</Link></header><section className={styles.operationStrip} aria-label="Today’s operations"><div><span>Active now</span><strong>{data.operations.activeLessons}</strong></div><div><span>Drivers ready</span><strong>{data.operations.availableDrivers}</strong></div><div><span>Vehicles available</span><strong>{data.operations.availableVehicles}</strong></div><div><span>Customers today</span><strong>{data.operations.customersToday}</strong></div><div><span>Classes today</span><strong>{data.operations.classesToday}</strong></div><div><span>KM today</span><strong>{data.operations.kilometresToday}</strong></div></section><div className={styles.dashboardGrid}><section><div className={styles.sectionTitle}><h2>Active lessons</h2><span>{data.activeLessons.length} running</span></div><div className={styles.activityTable}>{data.activeLessons.map(row=><article key={row.id}><b>Class {row.classNumber}</b><span>{row.customer.user.displayName}<small>{row.customer.customerCode}</small></span><span>{row.staff.user.displayName}<small>{row.vehicle.registrationNumber}</small></span><time>{new Date(row.startedAt).toLocaleTimeString("en-IN",{hour:"2-digit",minute:"2-digit"})}</time></article>)}{!data.activeLessons.length&&<p className={styles.empty}>No lesson is active right now.</p>}</div></section><aside className={styles.exceptionPanel}><h2>Needs attention</h2><Link href="/admin/training"><span>Pending kilometres</span><strong>{data.totals.pendingKilometres} km</strong><small>{e.pendingKilometres.length} customers</small></Link><Link href="/admin/training"><span>Expiring or expired</span><strong>{e.expiring.length+e.expired.length}</strong><small>{e.expiring.length} within 7 days</small></Link><Link href="/admin/payments"><span>Pending payments</span><strong>{money(data.totals.pendingPayments)}</strong><small>{e.pendingPayments.length} accounts</small></Link><Link href="/admin/complaints"><span>Open complaints</span><strong>{data.totals.openComplaints}</strong><small>Awaiting action or customer</small></Link><Link href="/admin/vehicles"><span>Vehicle warnings</span><strong>{data.totals.vehicleWarnings}</strong><small>Issues, status or documents</small></Link></aside></div><section className={styles.exceptionQueue}><div className={styles.sectionTitle}><h2>Closest deadlines</h2><span>Next 7 days and overdue</span></div>{[...e.expired,...e.expiring].slice(0,8).map(row=><article key={row.id}><b>{row.customerCode}</b><span>{row.customerName}<small>{row.courseName}</small></span><span>{row.classesCompleted}/{row.classesTotal} classes<small>{row.pendingKm} km pending</small></span><time>{row.deadlineAt?new Date(row.deadlineAt).toLocaleDateString("en-IN"):"—"}</time></article>)}{!e.expired.length&&!e.expiring.length&&<p className={styles.empty}>No deadline exception in the next 7 days.</p>}</section></div>}
